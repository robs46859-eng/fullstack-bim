DATABASE_URL=postgresql://user:pass@host:5432/stelarbim
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=AIza...
PORT=3000
NODE_ENV=development

# GCS — required for model file storage (presigned upload URLs)
# Create a bucket in Google Cloud Storage and make it public or use signed URLs
GCS_PROJECT_ID=your-gcp-project-id
GCS_BUCKET=your-bucket-name
