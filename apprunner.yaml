version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  command: npx tsx server/index.ts
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
